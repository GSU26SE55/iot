# Copy file này thành infra/.env trước khi chạy docker compose
# (.env không commit — đã liệt kê trong .gitignore root)

# Postgres / TimescaleDB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# RabbitMQ
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# Mosquitto MQTT broker — port mapping (host-side) — đổi nếu conflict
# Nếu host đã có Mosquitto local chạy 1883, đổi sang 21883 hoặc tắt instance local.
MQTT_PORT=1883
MQTT_TLS_PORT=8883
# User backend-bridge password — sinh qua: ./mqtt/mosquitto/bootstrap.sh
# Sau khi chạy bootstrap, copy password ra backend/.env.Docker → Mqtt__Password=...
