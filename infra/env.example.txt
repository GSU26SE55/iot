# Copy file này thành infra/.env trước khi chạy docker compose
# (.env không commit — đã liệt kê trong .gitignore root)

# Postgres / TimescaleDB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# RabbitMQ
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# EMQX Dashboard (http://localhost:18083)
EMQX_DASHBOARD_USER=admin
EMQX_DASHBOARD_PASS=public

# EMQX port mapping (host-side) — đổi nếu conflict
# Mặc định shift để tránh đụng Mosquitto local 1883 hay Postgres 8883/8083
EMQX_MQTT_PORT=11883
EMQX_MQTTS_PORT=18883
EMQX_WS_PORT=18030
EMQX_DASHBOARD_PORT=18083
