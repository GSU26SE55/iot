__pycache__/
*.pyc
.venv/
# Sprint 4 — anchor `/data/` để chỉ match data/ ở root iot/ (Python tools dataset).
# Rule cũ `data/` unanchored match TOÀN BỘ subfolder data/, ignore luôn
# firmware-esp32/data/ca_cert.pem.placeholder (cần commit như setup hint).
/data/
*.sqlite3
.DS_Store
Thumbs.db
logs/*.log

# Sprint 0 — Firmware
firmware-esp32/.pio/
firmware-esp32/.pioenvs/
firmware-esp32/.piolibdeps/
firmware-esp32/.vscode/
firmware-esp32/include/config.h

# Sprint 0 — Infra
infra/.env

# Production deployment secrets/runtime state stay on the VPS, never in Git.
deploy/production/*.env
!deploy/production/*.env.example

# loop-engine — sinh tự động, không commit
.loop/out/
.loop/cache/
.loop/runs/
.loop/guard.json
.loop/last-run.json
.loop/dispute.md
