### ✅ Đã Khôi Phục Code Firmware Gốc Thành Công

1. **Khôi phục (Revert)**:
   - Toàn bộ các chỉnh sửa liên quan đến tính năng sạc/xả BMS (`modbusWriteSwitch`, `cmd_logic`, `command_handler`, `main.cpp`) cũng như mô-đun thẻ nhớ SD trong thư mục `iot/firmware-esp32` đã được khôi phục nguyên bản về commit làm việc gần nhất.
   - Thư mục làm việc `firmware-esp32` hiện đã hoàn toàn sạch sẽ.

2. **Kết quả Biên Dịch**:
   - Firmware đã được biên dịch thành công 100%: `.pio\build\esp32-s3-devkitc-1\firmware.bin`
   - RAM: `24.7%` (80,916 bytes)
   - Flash: `19.0%` (1,245,217 bytes)

---

### 🚀 Nạp thử lại xuống mạch ESP32:
Bạn hãy thử nạp lại phiên bản firmware gốc này bằng lệnh:
```powershell
cd e:\Github\KLTN_dev-v2\iot\firmware-esp32
& "C:\Users\NEO\.platformio\penv\Scripts\pio.exe" run -t upload
```