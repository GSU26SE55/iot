[project]
name = "solar-battery-iot-gateway"
version = "0.1.0"
description = "IoT gateway simulator and hardware bridge for Solar Battery Monitoring"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

