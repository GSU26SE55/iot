# Copy to /opt/solar-iot/secrets/runtime.env, replace the password, then set
# mode 0640 and owner deploy:solar-runtime. It must match solar-secrets.
Mqtt__Username=backend-bridge
Mqtt__Password=CHANGE_ME
